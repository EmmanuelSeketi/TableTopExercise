# Public URL of the deployed app, used for QR codes and absolute links.
# Example: https://exercise.example.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Shared secret for the exercise state API (prevents unauthorized writes).
# Use the same value in both variables below.
NEXT_PUBLIC_EXERCISE_API_SECRET=change-me
EXERCISE_API_SECRET=change-me
